description: "virtualenv"
webPage: https://virtualenv.pypa.io/
platforms:
  Darwin:
    dependencies: [ pip ]
    packageManager: pip
    packageName: virtualenv
    isInstalled: virtualenv --version > /dev/null
