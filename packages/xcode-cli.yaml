description: 'Command line tools from XCode'
webPage: 'https://git-scm.com/'
platforms:
  Darwin:
    packageManager: 'shell'
    isInstalled: 'git --version > /dev/null'
    installCommand: 'xcode-select --install'

